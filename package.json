{
    "name": "vaulty",
    "version": "1.0.0",
    "description": "Save your commands, use them later.",
    "bin": {
        "vaulty": "./bin/vaulty.exe"
    },
    "scripts": {
        "build": "node scripts/build.js",
        "publish": "npm run build && npm publish"
    },
    "engines": {
        "node": ">=12"
    }
}
